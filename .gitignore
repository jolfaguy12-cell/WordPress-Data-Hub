# Server 2 secrets
server2/config.json
server2/media_sync_state.json
server2/event_sync_state.json
server2/.htpasswd
server2/.dashboard-password

# Runtime data — contains DB dumps (customer password hashes) and media files
# (possibly payment receipts with bank account info). MUST NEVER be committed.
data/

# Docker
docker/.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# WordPress generated
wordpress-plugin/behdashtik-mirror-connector/vendor/

# OS
.DS_Store
Thumbs.db
